# Frontend Configuration
# Copy this file to .env.local and adjust values as needed

# API Base URL (overrides all other settings if provided)
# VITE_API_URL=http://localhost:7892

# API Port (used if VITE_API_URL is not set)
# Default: 7892
# VITE_API_PORT=7892

# For local network access (mobile/other devices):
# VITE_API_URL=http://YOUR_IP:7892
# Or use: VITE_API_PORT=7892 (will auto-detect hostname)

# Note: All data paths are managed by the backend at ~/.kumiai/
# Frontend doesn't need to configure paths separately
